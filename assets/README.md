# assets

Image and demo assets for the project README.

- `demo.gif` — short screencast of `/research` running end-to-end. Referenced from the top of the project README. Record once per major version when the UX changes meaningfully.

Keep files small (`demo.gif` should be ≤ 5 MB if possible). If the demo grows, prefer a static screenshot here and link to a longer video in the release notes.
